// Placeholder shell. Task 5 of the v1 canvas launch plan replaces this with
// the ported D — Three-Plate prototype (national/state views, Senate layer,
// search, ZIP entry, district sheet). See
// docs/superpowers/plans/2026-09-19-v1-canvas-launch.md.
export default function App() {
  return (
    <div className="stage">
      <p>follow-the-donors — scaffold only, D — Three-Plate not yet wired.</p>
    </div>
  )
}
