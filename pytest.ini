[pytest]
# scripts/ is not a package — the stages are standalone numbered files, which
# means a name like 01_fetch is not even a legal module name. The helpers
# (_db, _report) are importable, and that is what the tests exercise.
pythonpath = scripts
testpaths = tests
